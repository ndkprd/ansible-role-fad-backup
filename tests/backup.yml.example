---

- name: Backup FortiADC configuration.
  hosts: all
  connection: local
  become: false
  ansible_user: ndkprd
  gather_facts: false
  vars:
    the_real_vars: "is located in group_vars"

  roles:
    # - ../../ansible-role-fortiadc-backup
    - ndkprd.fortiadc_backup
